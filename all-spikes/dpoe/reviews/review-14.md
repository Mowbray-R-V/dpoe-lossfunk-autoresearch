## Verification of the four Review-13 safeguards

**(2) Delta practical-support floor — SATISFIED.** `mve3-v2-protocol.md:76` defines `delta = 0.10 × (locked aggregate oracle-minus-none true-return AUC)` on the primary-estimand scale, and :85 correctly routes sub-`delta` H3a passes to `statistically-real-but-practically-null-v2` rather than clean support. This is the one item I can pass without qualification.

**(1) Grid re-entry bar — NOT SATISFIED for the failure that actually occurred.** The no-re-entry-without-new-protocol/review/sign-off clause exists only in the *engagement*-gate paragraph (:57). The gate that fired was the *solvability* gate, whose stop clauses (:37, :55) say only "stop" / "never open pilot or locked-v2 seeds" — they do not bar grid re-entry or retuning behind new protocol + independent review + human sign-off. `RESULTS.md:28` says "Do not expand the grid, retune" but a results file is not the frozen protocol. The safeguard must be mirrored into the solvability-gate paragraph.

**(3) Normalized-AUC flat-policy identity — NOT SATISFIED in substance.** Two problems. First, `gates.py:24` defines `auc()` but `oracle_none_metrics` never calls it — it computes `np.mean(oracle_eval["true_return"])` directly (`gates.py:53–54`). The unit test (`test_gates.py:10–11`) therefore asserts a property of a function the gate path does not execute, on a hand-written 3-element array. That is a tautology test, not a guard against gate/factorial divergence — which is precisely the risk protocol :76 waves away as true "by construction." Assert instead that `auc(curve) == mean(curve)` on an actual update-free policy evaluation, and that the factorial path calls the same `auc` with the same checkpoint spacing. (Also: `assertEqual` on floats → `assertAlmostEqual`.) Second, the identity strings live only in `write_frozen_config` (`gates.py:95–96`), which runs *only on pass*; the failure path (`gates.py:116`) writes neither, so no artifact records them.

**(4) Per-severity oracle-minus-none return-AUC — IMPLEMENTED IN CODE, ABSENT FROM THE REPORT.** `gates.py:71–74` computes `severity_oracle_minus_none_return_auc`, but `development-solvability-failed.json` was written 2026-07-21 18:18:20 and `gates.py` 18:42:54 — the persisted artifact contains no such field for any of the 27 candidates, and `RESULTS.md` reports no per-severity return-AUC. PROGRESS.md:11 honestly says the grid was not rerun to regenerate summaries, but the consequence is that the only v2 result in existence does not report safeguard 4, and it cannot be derived from stored data. Since the run is seed-deterministic and costs $0, a report-only regeneration with an audit asserting every pre-existing metric is bit-identical is the clean fix; disclose it as report-only, not re-entry.

## The four Review-13 REVISE items — none applied

- **Source competence 0.988:** the reported best tuple (`tau=0.18, beta0=0.10, clip=1.75`) has `source_competence_min: 0.988` (JSON:797). `RESULTS.md:23` and `PROGRESS.md:55` both still state **1.000**. Two human-facing reports carry a wrong number.
- **Boundary-solution disclosure:** absent everywhere (grepped PROGRESS/LEARNINGS/RESULTS). The best tuple sits on the grid corner — `tau` max, clip max, `beta0` min — and oracle success rises monotonically in `tau` and clip. A boundary optimum means the grid did not bracket the optimum; the honest claim is "not solvable **within the preregistered grid**," not "not solvable." This matters for the paper and independently argues against re-entry, which would now be outcome-driven.
- **Missed timing pre-check:** protocol :95 requires timing `tau=0.18/beta0=0.18/clip=±1.75` before the full grid. PROGRESS.md:48 records the complete grid run with no timing record anywhere. A skipped preregistered pre-check is a protocol deviation and must be disclosed even though it was harmless.
- **`pilot-v2` rename:** `results/mve3-pilot-v2/` still exists and is a *v1* pilot rerun, colliding with the reserved split tag that protocol :29 forbids opening. An auditor grepping `pilot-v2` finds a populated directory and would reasonably conclude the prohibition was breached.

## Additional (non-blocking)

- `gates.py:80` adds `source_competence_min >= .80` as a fifth *development* gate condition; the protocol's development gate (:48–54) lists four, with competence appearing only in the locked gate (:63). It is stricter and non-binding (all ≥0.988), so it changes nothing — but it is the same code-stricter-than-written-protocol pattern Reviews 08/10 already forced you to disclose for v1 (:19). `RESULTS.md:10` compounds it by listing competence as prespecified. Disclose, don't silently keep.
- Archived `config` blocks all show `eval_episodes: 200` because `asdict(cfg)` snapshots the pre-`replace` config (`gates.py:39` vs :113); the gate actually ran at 1,000. Misreports the run for reproducibility.

## On the stop decision itself

The decision is correct and I would not disturb it. No tuple passed, the shortfall is decisive at the best candidate (16.44% vs 25%; 11.36 pp vs 15 pp), `gates.py:114–118` refuses to write a frozen config or open pilot/locked seeds on failure, there is no locked CLI path, and classifying C3 as **untested — solvability gate failed** rather than killed is the right call under the plan's validity/kill separation. Fix the reporting, not the outcome.

VERDICT: REVISE — minimal blocking fixes: (a) mirror the new-protocol/review/sign-off re-entry bar into the solvability-gate paragraph; (b) make the flat-policy identity test exercise the gate's actual return path and assert shared `auc` usage with the factorial path; (c) regenerate the development artifact (report-only, bit-identical audit) so per-severity oracle-minus-none return-AUC is actually reported; (d) correct source competence to 0.988 in `RESULTS.md:23` and `PROGRESS.md:55`; (e) disclose the boundary solution and the skipped timing pre-check; (f) rename `results/mve3-pilot-v2/`.
