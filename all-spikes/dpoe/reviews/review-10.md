## Review 10: MVE 3 post-amendment re-verification

Scope: Review 09's B1 plus its three non-blocking items, across `analyze.py`, `RESULTS.md`, `verdict.json`, `LEARNINGS.md`.

### B1 — RESOLVED

All four artifacts now carry the deviation disclosure, and it is generated by code rather than hand-patched into the markdown (analyze.py:282), so a rerun cannot silently drop it.

- `RESULTS.md:75` — header is now "Verdict (post-review validity amendment)". The mislabel is gone.
- `RESULTS.md:77` — discloses that the kill rule *did* fire, names the three satisfied conditions (quality passed, full lost to O/P+O, I3 non-positive), states the override rationale (the rule presupposes a reachable goal), cites `reviews/review-08.md`, and asserts the amendment preceded any rerun. This is the full ask.
- `verdict.json:2-3` — `verdict_status: "post-review-validity-amendment"` and `preregistered_kill_rule_fired: true` make the deviation machine-readable.
- `LEARNINGS.md:56-60` — parallel disclosure, correctly framed as "a disclosed post-review preregistration deviation … rather than a claim that the locked test supported coupling."

### Review-09 non-blocking items — all addressed

1. H3b floor caveat: `RESULTS.md:63`, `LEARNINGS.md:70-71`. ✓
2. Review-08 carryovers: per-tier/per-severity trap prevalence and strength (`RESULTS.md:18-31`), minimum locked `reference_competence` = 1.000 (`RESULTS.md:10`), learned-tier oracle RMS-rescaling disclosed with its scope limit (`RESULTS.md:12`). ✓
3. Entropy/KL/checkpoint-array pointer: `RESULTS.md:33`. ✓

### Verification performed

I cross-checked the reported numbers against `locked-quality.json`: controlled AUROC 0.850/0.924/0.899 and Spearman 0.647/0.710/0.708 (gate ≥0.75/≥0.50, passes), leakage partial Spearman 0.0124, `reference_competence_min` 1.0, and every cell of the 12-row severity table match exactly. `RESULTS.md`'s floor figures (0.0004, 0.0002, 98.7%, max 0.0498) match `verdict.json` to rounding. Independent recomputation from `cell_summary.csv` was denied at the sandbox, so the floor statistics are verified as internally consistent between `verdict.json` and `RESULTS.md`, not recomputed from the raw cells — Review 08 established those magnitudes directly, so I did not treat this as a gap.

### Non-blocking

1. **`LEARNINGS.md:61-63` is textually corrupted.** A botched edit left a dangling fragment: "…reached the true goal: across" / "that the frozen-reference exploitation policy almost never reached the true goal: across". The sentence is duplicated and ungrammatical mid-paragraph. Content is correct; the prose is not submission-ready. Fix before the deck.
2. **`analyze.py:308` — `preregistered_kill_rule_fired` is defined as `not h3a_support`, which is broader than the preregistered rule.** `h3a_support` also folds in the quality gate, leakage guard, and `directions_ok`. A run where the controlled signal fails its gate would emit `preregistered_kill_rule_fired: true` alongside `conditional_c3: "untested-invalid-signal"` — self-contradictory, since the rule's antecedent requires quality to have passed. Harmless for this locked run (quality did pass, so the flag is true for the right reason), but it should be gated on `quality["controlled_locked_quality_pass"]` before any rerun reuses the field.
3. Threshold narration is split: `design_floor_detected` fires on mean < 0.01 (analyze.py:158) while `RESULTS.md:38` reports the fraction at ≤0.005. Both are disclosed, but stating the operative 1% trigger in the prose would stop a reader inferring 0.005 is the rule.
4. Still open from Review 08 #4: the per-severity-vs-prereg-aggregate quality-gate deviation is disclosed nowhere in `RESULTS.md`. It is stricter and passed anyway, but the artifact now has a natural home for it. Dead `path = Path()` also survives, at analyze.py:196.

The reclassification is now honest about being an override rather than an outcome, which was the whole of B1.

VERDICT: APPROVE
